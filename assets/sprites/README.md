Current sprites are original SVG pixel assets loaded by `src/scenes/BootScene.ts`.

`snes-wall-*.svg` files are original 32x32 process-blocker sprites for NO REPO, FIREWALL, PENDING, WAIT, HOLD, AMBIGUOUS, and DANN-E QUEUE wall enemies.

BootScene still has generated fallback textures so the MVP stays resilient if an asset is missing. Do not add copyrighted or ripped game assets here.
