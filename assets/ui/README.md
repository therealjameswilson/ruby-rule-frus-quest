Current UI panels are original SVG pixel assets loaded by `src/scenes/BootScene.ts`.

The reusable dialog, terminal, and reliability-meter systems use the NES-style palette and can later swap to refined checked-in PNG or SVG UI assets.
