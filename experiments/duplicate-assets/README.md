# Duplicate Asset Quarantine

These files are Finder-style duplicate SVG exports found during the LttP-caliber cleanup.

They are preserved here rather than deleted because the cleanup policy prefers quarantine over discard for ambiguous art:

- `public/assets/sprites/frus-volume 2.svg`
- `public/assets/sprites/manuscript 2.svg`

They are not referenced by the Phaser loader or gameplay code. Promote a file back into `public/assets/` only after comparing it with the canonical asset and confirming it improves pillar 7: cohesive 16-bit presentation.
