Duplicate Finder-style files quarantined during cleanup.

The `frusProgression 2.ts` and `frusProgression.test 2.ts` files differed
from the canonical tracked files, so they are preserved here for later manual
review. Other ` 2` files found in this pass were byte-identical to tracked
canonical files and were removed instead of committed as duplicate content.
