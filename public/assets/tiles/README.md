Current tiles are original SVG pixel assets loaded by `src/scenes/BootScene.ts`.

Future PNG or SVG tilesets can replace `office-tiles`, `archive-tiles`, `network-tiles`, and `vault-tiles` without changing scene logic.
